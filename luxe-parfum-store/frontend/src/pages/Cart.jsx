import { Link } from 'react-router-dom'; import { useCart } from '../context/CartContext';
export default function Cart(){const {items,removeFromCart,total}=useCart();return <div className='p-6 max-w-3xl mx-auto'>{items.map(i=><div key={i._id} className='flex justify-between border-b py-3'><span>{i.name}</span><span>${i.price}</span><button onClick={()=>removeFromCart(i._id)} className='text-red-500'>Remove</button></div>)}<p className='text-gold text-xl mt-4'>Total: ${total.toFixed(2)}</p><Link to='/checkout' className='gold-gradient text-black px-5 py-2 rounded inline-block mt-3'>Checkout</Link></div>}
